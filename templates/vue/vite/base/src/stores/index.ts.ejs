import { createPinia } from 'pinia'
<% if(persist){ %>import piniaPluginPersistedstate from 'pinia-plugin-persistedstate'<% } %>  

const pinia = createPinia()
<% if(persist){ %>pinia.use(piniaPluginPersistedstate)<% } %>  

export default pinia